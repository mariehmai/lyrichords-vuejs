version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10.16-browsers
    steps:
      - checkout
      - run:
          name: 'What branch am I on?'
          command: echo ${CIRCLE_BRANCH}
